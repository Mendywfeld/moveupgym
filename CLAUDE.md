# moveupgym.com

אתר תדמית לחוג אקרו-ג'ים לבנות של רחל וינפלד, קריית מלאכי.

## מבנה

- `index.html` — האתר כולו. קובץ יחיד, עצמאי לחלוטין: HTML, CSS ו-JS מוטמעים בפנים, כולל התמונות כ-base64. אין תלויות, אין build step, אין node_modules.
- `og-image.jpg` — תמונת התצוגה המקדימה לשיתוף בוואטסאפ ורשתות חברתיות.
- `favicon.png` — אייקון הלשונית.

## פרסום

האתר מוגש על ידי **Cloudflare Worker** בשם `moveupgym-site` (לא Pages). הוא מחובר לריפו הזה דרך ה-GitHub App של Cloudflare, וענף הייצור הוא `main`.

כל push ל-`main` מפעיל בנייה ופרסום אוטומטי ל-moveupgym.com. בפועל זה לוקח פחות מ-10 שניות. פקודת הפרסום היא `npx wrangler deploy`, תיקיית השורש היא `/`, ואין פקודת build — הקבצים מוגשים כמו שהם משורש הריפו.

אין צורך להריץ `wrangler` ידנית. commit ו-push זה כל מה שנדרש.

**אם push הצליח אבל האתר לא התעדכן** — אל תניח שזה קאש, ואל תחכה יותר מדקה. הכשל שקרה בעבר: ה-GitHub App של Cloudflare היה מוגדר `Only select repositories` בלי הריפו הזה ברשימה, ולכן פושים לא הפעילו בנייה בכלל — בשקט מוחלט, בלי שגיאה, ו-`git push` דיווח הצלחה.

הבדיקה הישירה היא להשוות את מה שנדחף למה שמוגש:

```bash
# מחרוזת ייחודית שהוספת בקומיט האחרון
curl -s https://moveupgym.com/ | grep -c 'הטקסט החדש'
```

אם זה 0 אחרי דקה, החיבור שבור. התיקון: https://github.com/settings/installations → Cloudflare Workers and Pages → Repository access → לוודא ש-`Mendywfeld/moveupgym` ברשימה.

**אל תשתמש ב-`/deployments` של GitHub API כאבחון.** Workers Builds לא רושם deployments ב-GitHub, ולכן הוא מחזיר מערך ריק גם כשהפרסום עובד מושלם. זו הובלה שולל.

## כללי עבודה

**השפה עברית, הכיוון RTL.** לכל טקסט באתר. אל תשנה `dir="rtl"`.

**אל תקרא את `index.html` במלואו. לעולם.** הוא כ-2MB בשורה אחת ארוכה, ורוב הנפח הוא תמונות base64. קריאה שלמה תמלא את חלון ההקשר ותכשיל את המשימה — במיוחד בסשן ענן מהטלפון.

**כך עורכים טקסט:**

1. אתר את המחרוזת עם grep, בלי לקרוא את הקובץ:
   ```bash
   grep -c 'הטקסט המדויק' index.html
   ```
2. שלוף רק את ההקשר סביבה:
   ```bash
   python3 -c "
   s=open('index.html',encoding='utf-8').read()
   i=s.find('הטקסט המדויק')
   print(repr(s[i-150:i+150]))
   "
   ```
3. החלף בעריכה נקודתית של אותה מחרוזת בלבד. אל תיגע בסגנון סביבה.

**שים לב לתווי ה-escape.** הטקסט של הדף יושב בתוך מחרוזת JavaScript, ולכן בקובץ הוא מופיע מוברח:

| בקובץ | המשמעות |
| --- | --- |
| `<\u002Fspan>` | תג הסגירה `</span>` |
| `\"` | גרש כפול `"` |
| `'` | אפוסטרוף — **לא** מוברח. חפש `שד' ירושלים` כרגיל |
| `\n` | שורה חדשה בתוך המחרוזת |

אל תתקן אותם ואל תמיר אותם לתווים רגילים — זה הפורמט התקין של הפלט המקומפל, ושינוי שלו ישבור את הדף.

זהירות עם `repr()` בפייתון: הוא מוסיף backslash משלו לאפוסטרוף, מה שנראה כאילו הקובץ מוברח כשהוא לא. אם אתה לא בטוח, בדוק תו-תו:
```bash
python3 -c "
s=open('index.html',encoding='utf-8').read()
i=s.find('הטקסט')
print([(c,'U+%04X'%ord(c)) for c in s[i-5:i+5]])
"
```

**אמת אחרי הפרסום** שהשינוי באמת עלה, במקום להניח:
```bash
curl -s https://moveupgym.com/ | grep -c 'הטקסט החדש'
```

**אל תעשה reformat או prettify לקובץ.** הוא נוצר מכלי בנייה ומכיל מחרוזות base64 ארוכות מאוד. שינוי עיצוב הקוד יהפוך כל diff עתידי לבלתי קריא. ערוך נקודתית בלבד.

**סגנון:** כל הסגנון inline ב-`style=`. אין stylesheet, אין CSS classes. תוספת סגנון חדש נכתבת inline באותו אופן.

**אל תוסיף תוכן שלא התבקש.** אל תמציא מחירים, תאריכים, שמות או ציטוטים. אם חסר מידע — שאל.

## פלטת צבעים

| שימוש | ערך |
| --- | --- |
| רקע | `#FFF9F3` |
| טקסט ראשי | `#1C2340` |
| טקסט משני | `#5B617D` |
| טקסט עמום | `#6E7490` |
| אדום מותג (CTA) | `#E23E5C` |
| צהוב מותג | `#FFC53D` |

## טיפוגרפיה

- כותרות: Rubik, משקל 700–800
- גוף: Assistant
- שתיהן נטענות מ-Google Fonts בראש הקובץ

## פרטים עובדתיים על העסק

אלה הנתונים הנכונים. אם משהו בקוד סותר אותם, הקוד טועה.

- מיקום: שד' ירושלים 36, קריית מלאכי
- מדריכה: רחל וינפלד
- קהל יעד: בנות

## מקור העיצוב

העיצוב המקורי נבנה ככלי עיצוב חיצוני ו-`index.html` הוא הפלט המקומפל שלו. שינויי טקסט וצבע — בצע ישירות כאן. שינויי מבנה גדולים או עיצוב מחדש — עדיף לחזור לכלי העיצוב ולייצא מחדש, אחרת שתי הגרסאות יתפצלו.
